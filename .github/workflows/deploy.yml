name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: linux  # Asegúrate de que las etiquetas coinciden con las del runner

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install sshpass
        run: |
          sudo apt-get update
          sudo apt-get install -y sshpass

      - name: Add server to known hosts
        run: ssh-keyscan -p 5664 -H ${{ secrets.DONWEB_SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to server
        env:
          SERVER_IP: ${{ secrets.DONWEB_SSH_HOST }}
          SERVER_USER: ${{ secrets.DONWEB_SSH_USERNAME }}
          SERVER_PASSWORD: ${{ secrets.DONWEB_SSH_PASSWORD }}
        run: |
          sshpass -p "$SERVER_PASSWORD" ssh -p 5664 -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP 'cd /root/docker-apps/digitalhouse/backend && git pull origin main && ./start_containers.sh'